# vision-mcp

MCP server that provides vision tools to text-only models, works with any
OpenAI-compatible provider. Supported image formats: PNG, JPEG, WebP, and
static GIF. Images may be supplied as a data URL, an absolute local file path,
or an HTTP/HTTPS URL.
