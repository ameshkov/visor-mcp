/**
 * Shared test setup run by Vitest before each test file (configured via
 * `setupFiles` in `vitest.config.ts`). Add global test setup here — env
 * defaults, shared fixtures, global mocks — that applies to every test.
 * This file is test infrastructure, not part of the production graph.
 */
