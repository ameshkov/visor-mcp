/**
 * Barrel exports for the `images/` service — image loading and validation.
 * External code MUST import from this barrel only.
 */
export { loadImage } from './images.js';
export type { ValidatedImage } from './images.js';
